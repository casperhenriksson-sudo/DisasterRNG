Quick project status. Run these commands:
- `git log --oneline -10`
- `git status`
- `Get-Process rojo`

Then read `.claude/PROBLEMS.md` for known issues.

Update `.claude/PROJECT_STATE.md` with current snapshot.

Report cleanly: latest commits, working tree status, Rojo running yes/no, any uncommitted changes.
